language: node_js
node_js:
  - "node"
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq awscli
script:
  - npm i -g electric-cli
  - npm install
  - electric build
  - aws s3 sync --region=us-west-2 dist/ s3://webda.io/ --acl public-read
  - electric deploy -e ghpages
