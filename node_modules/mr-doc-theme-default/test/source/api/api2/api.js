/**
 * @class Car
 * @example
 * var honda = new Car("Honda");
 */
class Car extends Vehicle {
	constructor(make){
			super(make);
	}
	/**
	 * Generates the files
	 * @example
	 * honda.accelerate();
	 */
	accelerate (){

	}

	/** 
	 * Some other function
	 * @example
	 * honda.honk();
	 */
	honk () {

	}
}